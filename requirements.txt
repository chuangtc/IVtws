# Core dependencies
python-dotenv==1.1.0
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0
selenium>=4.0.0
requests>=2.25.0
scipy == 1.15.3
beautifulsoup4 == 4.13.4
lxml == 5.4.0

# Optional visualization dependencies
plotly>=1.9.0
ipywidgets>=8.0.0
bqplot>=0.12.0
colour>=0.1.5

# Jupyter dependencies
notebook>=7.0.0
ipykernel>=6.0.0

# Development dependencies
pytest>=6.0.0
black
isort
flake8
mypy
pytest-cov

# Note: PhantomJS is a separate binary that needs to be installed on the system
# It cannot be installed via pip. Please install it separately via your system's package manager. 